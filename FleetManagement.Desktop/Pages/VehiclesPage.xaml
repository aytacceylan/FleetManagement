<Page x:Class="FleetManagement.Desktop.Pages.VehiclesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Form -->
        <Border Style="{StaticResource CardBorder}">
            <StackPanel>
                <TextBlock Text="Araç"
                            Style="{StaticResource PageTitleStyle}"/>

                <TextBlock Text="Yeni / Güncelle / Sil"
                            Style="{StaticResource PageSubtitleStyle}"/>

                <TextBlock Text="Plaka"/>
                <TextBox x:Name="PlateBox" Margin="0,6,0,10"/>

                <TextBlock Text="Marka"/>
                <TextBox x:Name="BrandBox" Margin="0,6,0,10"/>

                <TextBlock Text="Model"/>
                <TextBox x:Name="ModelBox" Margin="0,6,0,14"/>

                <StackPanel Orientation="Horizontal">
                    <Button Content="Yeni" Width="90" Click="New_Click"
                            Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}"/>
                    <Button Content="Kaydet" Width="90" Margin="8,0,0,0" Click="Save_Click"
                            Background="{StaticResource BrushAccent}" Foreground="White" BorderThickness="0"/>
                    <Button Content="Sil" Width="90" Margin="8,0,0,0" Click="Delete_Click"
                            Background="{StaticResource BrushDanger}" Foreground="White" BorderThickness="0"/>
                </StackPanel>

                <TextBlock x:Name="FormInfo" Margin="0,12,0,0"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
        </Border>

        <!-- List -->
        <Border Grid.Column="2" Style="{StaticResource CardBorder}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel>
                    <TextBlock Text="Araç Listesi"
                                Style="{StaticResource SectionTitleStyle}"/>
                    <TextBox x:Name="SearchBox" Width="260" Margin="12,0,0,0"
                             DockPanel.Dock="Right" TextChanged="SearchBox_TextChanged"
                             VerticalAlignment="Center"/>
                </DockPanel>

                <DataGrid x:Name="GridVehicles" Grid.Row="1" Margin="0,12,0,0"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          SelectionMode="Single" SelectionChanged="GridVehicles_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="70"/>
                        <DataGridTextColumn Header="Plaka" Binding="{Binding Plate}" Width="*"/>
                        <DataGridTextColumn Header="Marka" Binding="{Binding Brand}" Width="*"/>
                        <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*"/>
                        <DataGridTextColumn Header="Oluşturma" Binding="{Binding CreatedAt}" Width="180"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>